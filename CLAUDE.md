# Wall Family Cookbook — Claude Instructions

## Git Workflow

- **Always work directly on `main`.**
- Never create feature branches unless the user explicitly requests one.
- Commit and push to `main` after completing each task.
- Apply all changes to both development and production (they share the same codebase — pushing to `main` covers both).
